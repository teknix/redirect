# Tracked defaults (NO SECRETS)
# Values in here are defaults for the docker stack

# External port for the web service
APP_PORT=5006

# Flask settings
FLASK_DEBUG=0

# MongoDB settings
MONGO_URI=mongodb://mongo:27017/
DATABASE_NAME=redirect_db
COLLECTION_NAME=urls
LINK_SALT=convict the epstein class
